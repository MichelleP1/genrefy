<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.genrefy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".Settings.SettingsActivity"></activity>
        <activity android:name=".Genres.LibraryActivity" /> <!-- android:theme="@style/Theme.AppCompat.Light.NoActionBar"> -->
        <!-- android:theme="@style/Theme.Genrefy"> -->
        <activity android:name=".Authentication.SpotifyAuthActivity" />
        <activity android:name=".TestPlayerActivity" />
        <activity android:name=".Player.PlayerActivity" />
        <activity android:name=".Playlists.PlaylistActivity" />
        <activity android:name=".Genres.GenreListActivity" />
        <activity
            android:name="com.spotify.sdk.android.authentication.LoginActivity"
            android:launchMode="standard"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".Authentication.SignUpActivity" />
        <activity android:name=".Authentication.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>